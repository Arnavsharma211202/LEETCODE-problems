class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        bool flag=false;
        for(int i=0;i<nums.size();i++){
            if(i!=nums.size()-1){
            if(nums[i]==nums[i+1])
            flag=true;}
            cout<<flag<<endl;
            if(flag==true)
            break;
        }
        return flag;
        
    }
};
